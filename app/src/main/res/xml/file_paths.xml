<?xml version="1.0" encoding="utf-8"?>
<!-- Where the camera app may write the rider's proof-of-delivery photo (app cache only; deleted after upload). -->
<paths>
    <cache-path name="delivery_proof" path="delivery_proof/" />
</paths>
