# Android / Gradle
local.properties
# Optional Firebase Web OAuth id (copy from scripts/firebase_secrets.xml.example)
app/src/main/res/values/firebase_secrets.xml
.gradle/
build/
**/build/
captures/
*.iml
.idea/
.vscode/
.DS_Store

# CNN training — raw images are huge (~GB); clone locally or use Git LFS elsewhere
Cnn Training/Data/
Cnn Training/.mplconfig/
Cnn Training/ImageClassification/.venv/
Cnn Training/ImageClassification/.mplconfig/
Cnn Training/ImageClassification/outputs/
# Symlink to local Data/ folder; recreate after cloning (see README)
Cnn Training/ImageClassification/data

# Root / other Python virtual environments (do not commit)
.venv_tf/
.venv/
venv/

# Web dependencies and generated output
node_modules/
**/node_modules/
.vite/
**/.vite/
dist/
**/dist/

# Local recovery/backups and duplicate exported workspaces
_recovered_from_cursor_history/
_pre_recovery_backup/
Website_simple/

# Accidental root npm lockfile; the website lockfile is tracked in Website/
/package-lock.json

# Environment variables & secrets
.env
.env.*
!.env.example
*.env
*.env.local

